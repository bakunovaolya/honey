import {createTheme} from "@mui/material";

export  const myTheme = createTheme({
    palette:{
        primary:{
            main:'#FF8C00',
            contrastText: "#fff"
        },
        secondary:{
            main:'#fff'
        }
    }


})
